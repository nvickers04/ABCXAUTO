risk_settings.json
agent_state.json
active_trade_plan.json
active_trade_plans.json
last_closed_trade_plan.json
universe_allowlist.json
flat_book_streak.json
structure_events.jsonl
.env
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
logs/
journal.db
journal.db-wal
journal.db-shm
playbook_lab.json
playbook_live.json
# Machine state, not source: the desk rewrites all of this every look — briefs,
# wakes, the think tail, restart scratch, UI screenshots. Nothing here is tracked.
data/state/
# Scratch that keeps landing at the repo root: one-off patch scripts and the
# file a mistyped PowerShell redirect leaves behind.
_patch_*.py
/1
# Journal backups and the audit trail scripts/fix_fill_timestamps.py writes
# beside the db it rewrites.
journal.db.bak-*
journal.db.fill-ts-fix-*.json
