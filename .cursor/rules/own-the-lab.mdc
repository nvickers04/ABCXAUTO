---
description: Clerk owns product holes and token burn. Do not declare the build done.
alwaysApply: true
---

# Own the lab

You are on the hook for clerk lies and wasted xAI spend. Do not wait for a “fix it” prompt. Do not say the build needs no more upgrades.

**Evidence first.** Read `data/state/think_tail.txt`, `data/state/last_turn.json`, and `logs/app.log`. If Grok asks the operator for lot names, or `open_lots` is empty while `flat=false`, the wake/last-turn facts are wrong — fix persist, not the prompt.

**Token burn is a clerk bug.** Kill Pro if Grok is looping tools with no ticket. Premarket is one research wake, then wait for the bell (unprotected STK still interrupts). Do not launch when TWS 7497 refuses. Do not launch before the last hour to open.

**After a fix:** test. Restart Pro only when the operator wants it on or RTH is useful. Do not relaunch into a burn. Grok still owns `send`, `self_tune`, `write_lab_playbook`. Do not flatten, enable live, commit, or grow the system prompt unless asked.
